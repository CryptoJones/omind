when:
  - event: push
  - event: pull_request

steps:
  test:
    image: python:3.12-slim
    commands:
      - python -m pip install -U pip
      - pip install -e .[dev]
      - ruff check .
      - mypy src
      - pip-audit
      - pytest -v
