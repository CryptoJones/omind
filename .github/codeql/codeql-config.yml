name: "omind CodeQL config"

# SAST targets the shipped package only. Test files contain deliberate edge-case
# patterns that generate noise; analyzing src/ keeps findings actionable.
paths:
  - src

paths-ignore:
  - tests
